services:
  - type: web
    name: restoqr-frontend
    env: static
    staticPublishPath: dist
    buildCommand: npm run build
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
